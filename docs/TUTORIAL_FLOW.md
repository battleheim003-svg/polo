# Tutorial Flow

The tutorial introduces only the playable core:

- Cup runs are short deterministic paths.
- Each match starts from lineup preparation.
- Coach commands affect the match simulation.
- Wins grant reward choices.
- Camp, event, and market nodes shape the next match.
- The boss node decides the run.

Tutorial completion is stored in save `meta.tutorial_seen`.
